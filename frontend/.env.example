# Frontend environment variables
# Copy this file to .env and adjust values for your environment.

# API base URL for the frontend.
# Leave empty to use relative URLs (recommended when the reverse proxy
# routes /api/ to the backend on the same origin).
# Set to an absolute URL only if the API lives on a different origin.
VITE_API_BASE_URL=
