# Prompt 02A — Repository Structure

You are acting as a Senior Python Software Engineer.

Read `docs/ARCHITECTURE.md` and use it as the source of truth.

Create the repository directory structure only.

Requirements:

- Create all folders defined in the architecture.
- Create empty `__init__.py` files where appropriate.
- Create placeholder `README.md` files only if useful.
- Do NOT generate application logic.
- Do NOT create configuration files.
- Do NOT install dependencies.
- Do NOT implement Python classes or functions.

Only generate the project skeleton.

Modify only the repository structure.